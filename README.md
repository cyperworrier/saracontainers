
  # Build reference website

  This is a code bundle for Build reference website. The original project is available at https://www.figma.com/design/zW7TjytzCCd3tT5msq2VOl/Build-reference-website.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  